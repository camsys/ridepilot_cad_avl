<%= javascript_tag do %>
  var map;
  var n = <% @provider.region_nw_corner.y %>
  function initMap() {
    map = new google.maps.Map(document.getElementById('map'), {
      center: {lat: -34.397, lng: 150.644},
      zoom: 8
    });
  }
<% end %>

<%= javascript_include_tag "https://maps.googleapis.com/maps/api/js?v=3&libraries=places&key=#{ENV['GOOGLE_API_KEY']}&callback=initMap", async: true, defer: true %>