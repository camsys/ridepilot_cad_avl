= form_for :cad, url: ridepilot_cad_avl.cad_avl_path, html: { :id => "cad_date_filter_form", class: 'form-inline', style: "margin-bottom: 10px;" }, :method => :get do |f|
  = f.label :cad_day, "Select Date:"
  .input-group.datepicker-icon
    = f.text_field :date, :value => format_time_for_listing_day(@cad_day), class: "form-control datepicker", readonly: true
    .input-group-btn
      %button.btn.btn-default{:type => "button"}
        = image_tag "calendar-clock.png"
= render "shared/date_time_picker_javascript"
:javascript
  $(document).ready(function(){
    makeDatePickers();

    $('#cad_date').change(function() {
      $(this).parents('form').submit();
    });
  });
      
    